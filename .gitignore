# Μυστικά περιβάλλοντος — όλα τα αρχεία .env σε οποιονδήποτε φάκελο
.env
.env.*
# Κράτα πρότυπα στο repo (χωρίς secrets):
!.env.example
!.env.sample

.DS_Store

# Cursor — skills / τοπικές ρυθμίσεις IDE (όχι deploy στο server)
.cursor/
